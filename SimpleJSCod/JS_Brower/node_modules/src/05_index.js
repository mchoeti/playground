//"use strict";
/*jshint esversion: 6 */
const axios = require("axios").default;

// Simple 
/*
const productAPI = require("./api/05_product");
// const search = productAPI.search;
// // infos ist natürlich der Name vom Export Module in 05_prodict.js
const infos = productAPI.infos;
*/

// geht natürlich noch kompakter
const { search, infos } = require("./api/05_product");

search("Apple").then(function(res) {
    console.log(res);
});


infos(1103344).then(function(p) {
    console.log(p);
});

// Das würde auch klappen, dass ist half Fat arrow Schreibweise
// infos(1103344).then((p) => console.log(p));